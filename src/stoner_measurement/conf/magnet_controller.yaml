poll_interval_ms: 1000

connection:
  driver: ""
  transport: "Null (test)"
  address: ""

targets:
  field:
  current:

ramp:
  field_rate:
  current_rate:

limits:
  magnet_constant:
  max_current:
  max_field:
  max_ramp_rate:

stability:
  tolerance_t: 0.001
  window_s: 10.0
  min_rate: 0.0001
  unstable_holdoff_s: 2.0
